db:
  dsn: "root:root@tcp(localhost:13316)/byline"

redis:
  addr: "localhost:6379"

kafka:
  addrs:
    - "localhost:9094"

grpc:
  client:
    intr:
      addr: "localhost:8090"
      secure: false
      threshold: 100